---
description: Carga el "Skill" de Next.js 16 en el contexto actual para especializar al agente en las últimas features y breaking changes.
---

1. **Lee el archivo de conocimiento maestro** para "cargar" la habilidad en tu memoria:
   - Lee `src/claude/skills/nextjs-16-complete-guide/SKILL.md` (o la ruta correcta `.claude/skills/...`).
   
2. **Confirma la "Instalación de la Habilidad"** con un mensaje de sistema:
   - "🧠 **Skill Loaded**: Next.js 16 Expert Mode activated."
   - "Checking for typical Next.js 16 requirements (Node 20.9+, proxy.ts, cacheComponents)..."

3. **Verifica rápidamente** si el proyecto actual cumple con los requisitos básicos mencionados en el skill (opcional, pero recomendado):
   - Verifica la versión de `next` en `package.json`.
   - Busca si existe `middleware.ts` en lugar de `proxy.ts`.

4. **Ponte a la espera** de instrucciones de código.
